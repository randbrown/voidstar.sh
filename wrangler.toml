# Cloudflare Pages configuration
# Deploy via: wrangler pages deploy dist
# Or connect this repo in the Cloudflare dashboard with:
#   Build command:    npm run build
#   Output directory: dist
#   Node version:     22

name = "voidstar-sh"
compatibility_date = "2024-12-01"

[env.production]
name = "voidstar-sh"
